const { Camer } = require(__dirname + '/../../../db/models/Camer');
const { Fields } = require(__dirname + '/../../../db/Fields');

class salesonetodk extends Camer {
}

salesonetodk.jsonSchema;
module.exports = {salesonetodk};